<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.hsd.core.board.repository.BriefingRepository">

    <resultMap id="briefingDateResult" type="briefingDate">
        <id property="idx" column="IDX"/>
        <result property="type" column="TYPE"/>
        <result property="joinDate" column="JOINDATE"/>
    </resultMap>

    <insert id="insertBriefing">
        INSERT INTO TB_BRIEFING
        (NAME, HP, JOINLOC, JOINNUM, JOINDT, LUNCHYN, REGDT)
        VALUES
        (#{name}, #{hp}, #{joinLoc}, #{joinNum}, #{joinDt}, #{lunchYn}, NOW())
    </insert>
    <select id="listBriefingDate" resultMap="briefingDateResult">
        SELECT * FROM TB_BRIEFING_DATE A
        WHERE A.JOINDATE > NOW()
        ORDER BY A.JOINDATE ASC
    </select>
</mapper>