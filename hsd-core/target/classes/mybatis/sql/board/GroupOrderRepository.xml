<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.hsd.core.board.repository.GroupOrderRepository">

    <insert id="insertBoard">
        INSERT INTO TB_GROUP_ORDER (NAME, EMAIL, HP, TITLE, CONTENTS, ATTACH_FILE, ATTACH_NAME, REGDT, MARKETING_AGREE, STORE_AGREE) VALUES
        (#{name}, #{email}, #{hp}, #{title}, #{contents}, #{attachFile}, #{attachName}, NOW(), #{marketing_agree}, #{store_agree})
    </insert>
</mapper>